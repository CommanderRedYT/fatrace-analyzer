# fatrace-analyzer

This is a webui for parsing and analyzing / visualizing traces from `fatrace`

To generate such a trace, run the following command: 

```bash
$ fatrace -o /path/to/trace.json -j
```
